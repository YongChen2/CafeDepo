export function DemoBanner() {
  return (
    <div
      className="sticky top-0 z-50 bg-fg text-bg font-mono text-xs uppercase px-4 py-3 tracking-wide"
      style={{ borderLeft: "4px solid var(--color-accent)" }}
    >
      DEMO REŽIM — ZMĚNY SE NEUKLÁDAJÍ
    </div>
  );
}
